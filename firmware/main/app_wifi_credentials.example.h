#ifndef APP_WIFI_CREDENTIALS_H
#define APP_WIFI_CREDENTIALS_H

/*
 * Copy this file to app_wifi_credentials.h and edit the copy.
 * app_wifi_credentials.h is ignored by Git and must never be committed.
 */
#define APP_WIFI_SSID "YOUR_WIFI_NAME"
#define APP_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#endif
